import { createTheme, fontFamily } from "@mui/system";

const theme = createTheme({
    typography:{
        fontFamily:"Poppins"
    },
    palette:{
        primary:{
            main: "#34C94B"
        }
    }
})
export default theme;